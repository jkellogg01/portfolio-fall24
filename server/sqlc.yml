version: "2"
sql:
  - engine: "postgresql"
    queries: "sql/queries.sql"
    schema: "sql/schema"
    gen:
      go:
        out: "database"
        sql_package: "pgx/v5"
        emit_json_tags: true
